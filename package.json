{
  "name": "simplestay",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:landlord": "yarn workspace landlord build",
    "dev:landlord": "yarn workspace landlord dev",
    "dev:tenant": "yarn workspace tenant dev",
    "build:tenant": "yarn workspace tenant build"
  },
  "packageManager": "yarn@3.2.1",
  "engines": {
    "yarn": ">=1.22.4"
  },
  "cacheDirectories": [
    ".yarn/cache"
  ],
  "workspaces": [
    "apps/*",
    "libs/*"
  ]
}
